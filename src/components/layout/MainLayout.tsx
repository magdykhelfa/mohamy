import { ReactNode } from 'react'; import { Sidebar } from './Sidebar'; import { Header } from './Header'; interface MainLayoutProps { children: ReactNode; user?: any; } export function MainLayout({ children, user }: MainLayoutProps) { const handleLogout = () => { localStorage.removeItem('current_lawyer_user'); window.location.reload(); }; return ( <div className="min-h-screen bg-background"> <Sidebar user={user} onLogout={handleLogout} /> <div className="mr-64 transition-all duration-300"> <Header user={user} onLogout={handleLogout} /> <main className="p-6 animate-in fade-in duration-500"> {children} </main> </div> </div> ); }